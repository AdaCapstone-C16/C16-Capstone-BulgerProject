//This is the user profile page 
//currently only shows "change password"
import UserProfile from '../components/profile/user-profile';

const ProfilePage = () => {
  return <UserProfile />;
};

export default ProfilePage;
